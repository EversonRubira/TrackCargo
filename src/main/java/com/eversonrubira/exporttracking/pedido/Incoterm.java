package com.eversonrubira.exporttracking.pedido;

// Incoterms 2020, conjunto fechado e oficial (ICC) - lista completa
// incluida de proposito, mesmo so usando CFR/FOB hoje: e um padrao
// internacional fixo, nao especulacao de uso futuro.
public enum Incoterm {
    EXW, FCA, FAS, FOB, CFR, CIF, CPT, CIP, DAP, DPU, DDP
}
