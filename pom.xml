<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.1</version>
        <relativePath/>
    </parent>

    <groupId>com.eversonrubira.exporttracking</groupId>
    <artifactId>export-tracking</artifactId>
    <version>0.0.1-SNAPSHOT</version>
    <name>export-tracking</name>
    <description>Rastreamento de pedidos de exportacao de commodity (F01)</description>

    <properties>
        <java.version>21</java.version>
    </properties>

    <!-- spring-boot-dependencies (importado via parent) forca Groovy
         5.0.8 via groovy-bom, mas REST Assured 5.5.x foi construido
         contra Groovy 4.0.22 - o MOP do Groovy 5 quebra internamente no
         REST Assured (NullPointerException em ClosureMetaClass ao
         montar requests PATCH). So um override de propriedade nao
         resolve, porque o placeholder ${groovy.version} dentro do
         groovy-bom importado ja foi interpolado com o valor do proprio
         POM do Boot quando ele foi publicado - precisa fixar a versao
         explicitamente aqui pra vencer o dependencyManagement herdado. -->
    <dependencyManagement>
        <dependencies>
            <dependency>
                <groupId>org.apache.groovy</groupId>
                <artifactId>groovy</artifactId>
                <version>4.0.22</version>
            </dependency>
            <dependency>
                <groupId>org.apache.groovy</groupId>
                <artifactId>groovy-xml</artifactId>
                <version>4.0.22</version>
            </dependency>
            <dependency>
                <groupId>org.apache.groovy</groupId>
                <artifactId>groovy-json</artifactId>
                <version>4.0.22</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-flyway</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>

        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- F03: PDF de status. Versao 3.0.5 e a atual estavel (conferida
             via maven-metadata.xml antes de fixar) - pacote mudou de
             com.lowagie.text.* (legado, herdado do iText 2.x) pra
             org.openpdf.text.* nessa major version. Licenca permanece
             LGPL/MPL (dual), motivo original de escolher OpenPDF em vez
             do iText moderno (AGPL). -->
        <dependency>
            <groupId>com.github.librepdf</groupId>
            <artifactId>openpdf</artifactId>
            <version>3.0.5</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-webmvc-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>io.rest-assured</groupId>
            <artifactId>rest-assured</artifactId>
            <version>5.5.6</version>
            <scope>test</scope>
        </dependency>

        <!-- So pra teste: o PdfTextExtractor do proprio OpenPDF 3.0.5
             (org.openpdf.text.pdf.parser) devolve "?" no lugar de
             qualquer caractere acentuado (a, e, i, o, u, c com acento/
             cedilha) em fontes padrao nao embutidas - confirmado
             comparando com PyMuPDF (motor de extracao independente),
             que le o mesmo PDF corretamente. Ou seja, o PDF gerado
             esta certo, so o extrator do OpenPDF e que nao decodifica
             WinAnsi acima de 0x7F de volta pra Unicode. no PdfBox
             (Apache 2.0, escopo test apenas, nao afeta producao/
             licenca/runtime) faz a extracao de verdade pros testes
             confirmarem o motivo/status no texto do PDF - versao 3.0.8
             conferida via maven-metadata.xml como a mais recente antes
             de fixar. -->
        <dependency>
            <groupId>org.apache.pdfbox</groupId>
            <artifactId>pdfbox</artifactId>
            <version>3.0.8</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
            </plugin>
        </plugins>
    </build>

</project>
